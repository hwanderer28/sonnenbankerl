# ==============================================================================
# Sonnenbankerl Environment Configuration
# ==============================================================================
# Copy this file to .env and fill in the values
# NEVER commit .env to git!

# Database Configuration
# ------------------------------------------------------------------------------
POSTGRES_USER=postgres
POSTGRES_PASSWORD=CHANGE_THIS_TO_A_SECURE_PASSWORD
POSTGRES_DB=sonnenbankerl

# Database URL (for backend connection)
# Note: Use 'postgres' as hostname when connecting from Docker containers
DATABASE_URL=postgresql://postgres:CHANGE_THIS_TO_A_SECURE_PASSWORD@postgres:5432/sonnenbankerl

# API Configuration
# ------------------------------------------------------------------------------
API_PORT=8000
ENVIRONMENT=production
ALLOWED_ORIGINS=*

# External APIs (Optional - add when available)
# ------------------------------------------------------------------------------
# GEOSPHERE_API_KEY=your_api_key_here
# GEOSPHERE_API_URL=https://dataset.api.hub.geosphere.at/

# Deployment Configuration (for VPS)
# ------------------------------------------------------------------------------
# Domain for the API (update with your actual domain)
DOMAIN=sonnenbankerl.ideanexus.cloud

# Notes:
# - Generate a secure password: openssl rand -base64 32
# - Postgres is exposed on port 5435 externally (to avoid conflicts)
# - Traefik network name: traefik_proxy
# - TLS certificate resolver: letsencrypt
